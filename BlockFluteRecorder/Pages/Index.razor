@page "/"
<Canvas @ref="canvas"></Canvas>
<Palette @ref="palette" AddNote="AddNote"></Palette>






@code {
    private Canvas canvas;
    private Palette palette;

    private void AddNote(Note note)
    {
        canvas.AddNote(note);
    }



}
